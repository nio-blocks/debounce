{
  "nio/Debounce": {
    "Output": "The first signal for each group in **group_by**, every **interval**.",
    "Description": "After a signal flows through the block, filter out following signals for x seconds.",
    "Category": "SignalModifier",
    "Properties": {
      "interval": {
        "description": "Amount of time to wait before allowing another signal in a matching group."
      },
      "group_by": {
        "description": "The value by which signals are grouped."
      }
    },
    "Version": "1.0.0",
    "Input": "Any list of signals.",
    "Commands": {},
    "Dependencies": [
      "None"
    ]
  }
}
